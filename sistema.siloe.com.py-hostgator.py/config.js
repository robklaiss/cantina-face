// Cantina Face — Backend Admin Configuration
// Apuntar al directorio /api del backend PHP (mismo host)
// Si se sirve desde otro host, cambiar a la URL completa del API.
window.CANTINA_BACKEND_CONFIG = {
    API_BASE: "/api",
};
